---
type: Package
name: TestCases
---

Tool Validation test cases for the Syscribe CLI validator.
All tests are `testLevel: L3` (system-level — exercised by running the tool against crafted model directories).
Each test case traces to one requirement via `verifies:`.
