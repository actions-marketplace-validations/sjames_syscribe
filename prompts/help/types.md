# types — element type inventory with counts

## SYNOPSIS
    syscribe -m <root> types

## DESCRIPTION
Lists every element type present in the model with a count of each. Use
`syscribe spec types` for the full type catalogue (including types not yet used).

## EXAMPLES
    syscribe -m model/ types

## SEE ALSO
    untyped, list, spec types
